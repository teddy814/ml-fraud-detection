# ml-fraud-detection
ML pipeline for fraud detection using preprocessing, feature engineering, and an XGBoost model with SMOTE for class imbalance. Includes evaluation (ROC, PR curves), explainability via SHAP, and a Streamlit dashboard.
